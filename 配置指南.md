# PubMed文献搜索配置指南

## 🎯 快速开始

### 方案1：修改脚本中的搜索词（推荐）

编辑 `pubmed.py` 文件，在配置区域找到这行：

```python
SEARCH_TERM = """
("Medium-chain triglycerides" OR "MCT" OR "Caprylic acid") AND ("Weight loss" OR "Body composition" OR "Fat mass") AND ("Adults"[Mesh] OR "Adult") AND ("Obesity"[Mesh] OR "Overweight"[Mesh] OR "Obesity" OR "Overweight") NOT ("Diabetes Mellitus"[Mesh] OR "Diabetes" OR "Hypertension"[Mesh] OR "High blood pressure" OR "Cardiovascular Diseases"[Mesh] OR "Metabolic Syndrome"[Mesh] OR "Neoplasms"[Mesh] OR "Cancer" OR "Pregnancy" OR "Pregnant" OR "Child" OR "Adolescent")
"""
```

将您的搜索词替换进去即可！

### 方案2：交互式输入

运行 `python pubmed.py` 时，如果您不想使用默认搜索词，可以：

1. 直接运行脚本
2. 修改脚本中的 `SEARCH_TERM` 变量
3. 或者让我帮您实现交互式输入

## 📝 常用搜索词示例

### 营养学研究
```python
# Omega-3脂肪酸研究
SEARCH_TERM = '("Omega-3" OR "EPA" OR "DHA") AND ("Cardiovascular" OR "Heart disease") AND ("Randomized Controlled Trial"[Publication Type])'

# 维生素D补充剂研究
SEARCH_TERM = '("Vitamin D" OR "25(OH)D") AND ("Bone health" OR "Osteoporosis") AND ("Adults"[Mesh])'

# 蛋白质补充与肌肉合成
SEARCH_TERM = '("Protein supplement" OR "Whey protein") AND ("Muscle synthesis" OR "Anabolism") AND ("Athletes"[Mesh] OR "Exercise")'
```

### 医学研究
```python
# 糖尿病药物研究
SEARCH_TERM = '("Metformin" OR "Diabetes medication") AND ("Blood glucose" OR "HbA1c") AND ("Type 2 Diabetes"[Mesh])'

# 降压药效果研究
SEARCH_TERM = '("Antihypertensive" OR "Blood pressure medication") AND ("Hypertension"[Mesh]) AND ("Cardiovascular outcomes")'

# 抗抑郁药物研究
SEARCH_TERM = '("Antidepressant" OR "SSRI" OR "SNRI") AND ("Depression"[Mesh]) AND ("Efficacy" OR "Effectiveness")'
```

### 运动科学研究
```python
# 运动与健康
SEARCH_TERM = '("Exercise" OR "Physical activity") AND ("Weight loss" OR "Obesity"[Mesh]) AND ("Adults"[Mesh])'

# 有氧运动vs力量训练
SEARCH_TERM = '("Aerobic exercise" OR "Resistance training") AND ("Cardiovascular fitness" OR "Muscle strength") AND ("Adults"[Mesh])'
```

## 🔧 搜索语法说明

### 基本语法
- **AND**: 两个条件都必须满足
- **OR**: 满足其中一个条件即可
- **NOT**: 排除包含该条件的结果
- **""**: 精确匹配短语
- **[]**: 使用MeSH主题词

### 常用MeSH主题词
- `[Mesh]` - 使用医学主题词
- `[Publication Type]` - 限定文献类型
- `[Age Group]` - 限定年龄组

### 常用限定条件
- `"Randomized Controlled Trial"[Publication Type]` - 仅RCT研究
- `"Meta-Analysis"[Publication Type]` - 仅Meta分析
- `"English"[lang]` - 仅英文文献
- `AND ("2010/01/01"[PDAT] : "2024/12/31"[PDAT])` - 限定发表时间

## 🎛️ 其他可配置参数

在 `pubmed.py` 开头区域，您还可以调整：

```python
# 检索关键词
SEARCH_TERM = "您的搜索词"

# 想要获取的文献数量
MAX_RESULTS = 50  # 默认50篇，可调整

# 您的邮箱（PubMed API要求）
Entrez.email = "your-email@example.com"

# AI API配置
API_KEY = "您的API密钥"
ENABLE_WEB_SEARCH = True  # 是否启用web search功能
REQUEST_DELAY = 1.0  # API请求间隔（秒）
```

## 💡 搜索优化建议

1. **具体化搜索**: 使用特定的主题词而非泛化词汇
2. **合理限制**: 使用AND、NOT等操作符缩小搜索范围
3. **避免过度限制**: 避免使用过多限制条件导致结果过少
4. **多次尝试**: 如果结果不理想，调整搜索词重新尝试

## 🚀 使用步骤

1. **编辑搜索词**: 修改 `pubmed.py` 中的 `SEARCH_TERM`
2. **运行测试**: 先运行 `python pubmed.py test` 确保功能正常
3. **执行搜索**: 运行 `python pubmed.py` 开始搜索
4. **查看结果**: 查看生成的Excel文件

需要我帮您修改搜索词或实现其他功能吗？